{
  "name": "server",
  "version": "1.0.0",
  "description": "News-App",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build-client": "cd ../client && npm install && npm run build && rm -rf ../server/client/build && cp -r build ../server/client/build",
    "start:prod": "NODE_ENV=production node index.js",
    "start:pm2": "pm2 start index.js --name ssnnews",
    "deploy": "npm run build-client && npm run start:prod"
  },
  "repository": {
    "type": "git",
    "url": "SSN"
  },
  "keywords": [
    "News"
  ],
  "author": "sanju.g, pooja.s",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.11.0",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.1",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.13.1",
    "node-cron": "^3.0.3",
    "validator": "^13.12.0",
    "xss": "^1.0.15"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
